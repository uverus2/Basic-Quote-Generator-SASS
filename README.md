# Basic Quote Generator SASS Quote generator 

## adding more quotes is done via the array called quotes 

### If changes made to the style use the    sass --watch index.scss:index.css --style compressed (for minification) 
